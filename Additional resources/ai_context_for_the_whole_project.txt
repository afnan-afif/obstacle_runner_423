# MASTER PROJECT CONTEXT: 3D OPENGL OBSTACLE RUNNER

## 1. PROJECT RULES & CONSTRAINTS
- This is a 3D Computer Graphics university project built in Python using legacy OpenGL and GLUT.
- NO Shaders and NO Textures. 
- Limited to basic shapes (`glutSolidCube`, `gluCylinder`, `gluSphere`, `GL_QUADS`) and transformations (`glTranslatef`, `glRotatef`, `glScalef`). 
- Must use `glEnable(GL_DEPTH_TEST)`.
- The project must be contained entirely within a SINGLE `.py` file. 
- All code generated by the AI MUST be converted directly into Python.

## 2. PROJECT ARCHITECTURE & COMPLEXITY
To meet grading standards, this project is highly complex:
- **Member 1 (World):** Builds a 3D translating arcade tunnel, handles dynamic color interpolation for a day/night cycle, and manages a dynamic FOV "Warp Speed" camera effect.
- **Member 2 (Player):** Builds a hierarchical animated avatar (multi-part body with swinging limbs), implements smooth kinematics (lane switching, parabolic jumping), and handles a "Gravity Inversion" state where the player runs on the ceiling.
- **Member 3 (Obstacles):** Manages arrays of obstacles and power-ups using pattern-based spawning, implements morphing/pulsing obstacle animations, and writes dynamic 3D Axis-Aligned Bounding Box (AABB) collision logic.

## 3. "ZONED CODING" REPOSITORY RULES
Because three people are working in one file, the AI must strictly respect these comment zones and NEVER write code outside the requested zone:
- ZONE 0: `# === GLOBAL VARIABLES ===`
- ZONE 1: `# === MEMBER 1: WORLD & CAMERA ===`
- ZONE 2: `# === MEMBER 2: PLAYER ===`
- ZONE 3: `# === MEMBER 3: OBSTACLES & ITEMS ===`
- ZONE 4: `# === MAIN GAME LOOP ===`

AI INSTRUCTION: Acknowledge you understand this master context. Ask the user which Member they are and what specific function they want to write.